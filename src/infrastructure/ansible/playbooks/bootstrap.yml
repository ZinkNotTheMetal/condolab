---
- name: Bootstrap homelab Debian hosts
  hosts: homelab
  become: true
  gather_facts: true
  roles:
    - role: common
    - role: infisical_cli
      when: infisical_cli_install | default(false) | bool
    - role: docker
      when: docker_install | default(false) | bool
