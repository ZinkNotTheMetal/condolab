http:
  routers:
    qbittorrent:
      rule: Host(`qbittorrent.zinkzone.tech`)
      entryPoints:
        - websecure
      tls:
        certResolver: cloudflare
      middlewares:
        - security-headers
      service: qbittorrent

  services:
    qbittorrent:
      loadBalancer:
        passHostHeader: true
        servers:
          - url: http://host.docker.internal:8080
